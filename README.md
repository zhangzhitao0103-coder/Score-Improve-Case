# 有道领世名师团提分案例

高中学段销售/运营素材检索页，支持按老师、分数段、省份快速筛选提分案例图片，并预览、复制素材。

## 本地运行

直接打开 `index.html`，或启动静态服务：

```bash
python -m http.server 8088
```

然后访问 `http://127.0.0.1:8088/index.html`。

## 数据来源

页面数据由 `generate-data.js` 从 `提分案例汇总/` 目录生成到 `data.js`。
